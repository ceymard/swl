{
  "name": "swl-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "watch": "concurrently -c red,green,blue,yellow,magenta -n swl,csv,json,sqlite,test 'cd swl && npm run watch' 'cd swl.csv && npm run watch' 'cd swl.json && npm run watch' 'cd swl.sqlite && npm run watch' 'cd swl.test && npm run watch'"
  },
  "workspaces": [
    "./swl*"
  ],
  "dependencies": {
    "concurrently": "^3.5.1"
  }
}
